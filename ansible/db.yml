---
  - name: Configure MongoDB
    hosts: db
    become: true

    vars:
      mongo_bind_ip: 0.0.0.0

    roles:
    - db
  
    handlers:
    - name: restart mongod
      service: name=mongod state=restarted
